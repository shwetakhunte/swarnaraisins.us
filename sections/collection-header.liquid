{% assign id = section.id  %}
{% assign show_full_width = section.settings.show_full_width %}
<section id="laber_{{id}}" style="--alo-container-width:{{ section.settings.custom_width }}px;">
<div class="container{%if show_full_width%} full-width{%endif%}">
<div class="collection-hero{% if section.settings.show_collection_image and collection.image %} collection-hero--with-image{% endif %}">
  <div class="collection-hero__inner page-width">
    <div class="collection-hero__text-wrapper">
      {%- if section.settings.show_collection_image and collection.image -%}
      {% assign image = collection.image %}
      <div class="collection-hero__image-container relative" style="--alo-height-pc:{{ section.settings.h_des }}; --alo-height-tb:{{ section.settings.h_tb }};--alo-height-mb:{{ section.settings.h_mb }}">
          {% render 'banner-image', image: image, image_class: 'w-full h-full' %}
      </div>
      {%- endif -%}
      {%- if collection.title != blank -%}
        <h1 class="collection-hero__title">
          <span>{{- collection.title | escape -}}</span>
        </h1>
      {%- endif -%}
    </div>
  </div>
</div>
</div>
</section>
{%- render 'section_style' -%}
{% schema %}
{
  "name": "Collection header",
  "class": "spaced-section spaced-section--full-width",
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.main-collection-banner.settings.paragraph.content"
    },
    {
    "type": "checkbox",
    "id": "show_full_width",
    "default": false,
    "label": "t:sections.main-collection-banner.settings.show_full_width.label"
    },
    {  
        "type": "range",  
        "id": "custom_width",  
        "label": "Custom width",
        "min": 1170,
        "max": 1920,
        "step": 10,
        "unit": "px",
        "default": 1410 
    },
    {
      "type": "checkbox",
      "id": "show_collection_image",
      "default": false,
      "label": "t:sections.main-collection-banner.settings.show_collection_image.label",
      "info": "t:sections.main-collection-banner.settings.show_collection_image.info"
    },
    {
        "type": "text",
        "id": "h_des",
        "label": "Desktop Large",
        "placeholder": "150px",
        "default": "150px"
      },
      {
        "type": "text",
        "id": "h_tb",
        "label": "Tablet (<= 1024px)",
        "placeholder": "100px",
        "default": "100px"
      },
      {
        "type": "text",
        "id": "h_mb",
        "label": "Mobile (<= 767px)",
        "placeholder": "150px",
        "default": "150px"
      },
    {
    "type": "color",
    "id": "cl_bg",
    "label": "Background color",
    "default": "#fff"
    },

    {
    "type": "header",
    "content": "== Setting PC",
    "info":"Margin"
    },	
    {
    "type": "text",
    "id": "mg",
    "label": "Margin",
    "info":"Margin top, margin right, margin bottom, margin left. If you not use to blank"
    },
    {
    "type": "text",
    "id": "pd",
    "label": "Padding",
    "info":"Padding top, padding right, padding bottom, padding left. If you not use to blank"
    },
    {
    "type": "header",
    "content": "== Design Mobile Options"
    },
    {
    "type": "text",
    "id": "mg_mb",
    "label": "Margin"
    },
    {
    "type": "text",
    "id": "pd_mb",
    "label": "Padding"
    }
  ]
}
{% endschema %}
